# Notebooks

Notebooks may audit data and explore already-generated results. They must not be
the only way to produce a headline result. The planned notebooks are
`00_data_audit.ipynb` and `01_result_exploration.ipynb`, to be added when their
corresponding reproducible pipelines exist.

